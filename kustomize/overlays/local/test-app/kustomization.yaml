apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
#--------------------------------------------------
# Change value
#--------------------------------------------------
namePrefix: test-app-
commonLabels:
  app: test-app
  env: local
images:
  - name: image
    newName: main
    newTag: latest
#--------------------------------------------------
# Base
#--------------------------------------------------
bases:
  - ../../../base/deployment
resources:
  - ingress.yaml
  - service.yaml
patchesStrategicMerge:
  - patch-deployment.yaml
secretGenerator:
- name: secrets
  envs:
  - secrets.env
