apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
#--------------------------------------------------
# Change value
#--------------------------------------------------
namePrefix: test-app-
commonLabels:
  app: test-app
  env: local
images:
  - name: image
    newName: xxxxxxxxxxxxxx.dkr.ecr.ap-northeast-1.amazonaws.com/main
    newTag: latest
#--------------------------------------------------
# Base
#--------------------------------------------------
bases:
  - ../../../base/deployment
resources:
  - target-group-binding.yaml
  - service.yaml
patchesStrategicMerge:
  - patch-deployment.yaml
secretGenerator:
- name: secrets
  envs:
  - secrets.env
